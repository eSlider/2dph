.venv/
var/
__pycache__/
*.pyc
*.lbug
*.lbug.*
.cache/
.DS_Store
*.env
.env
# testdata fixtures may ship .env (committed, not secrets)
!internal/config/testdata/**/.env
.secrets/
lib-ladybug/
go.work.local
models/
# Purged from git history. Do not re-add.
docs/crm-associations-proof.md

# mount scaffold for the 8TB volume, never part of the repo
mnt/

# go build ./bin/mail/sync.go drops a binary named `sync` in cwd
/sync

# native brain binaries built by scripts/stack/serve-brain
/bin-build/

# local compiled binaries dropped in repo root (go run -o . / go build)
/brain
/contacts
/web

# knowledge-mesh corpus dump (data, not source)
/corpus/

# machine-local config overrides (never committed; CI guard on absolute paths)
etc/brain/config.local.yml

# AI-agent global config lives at ~/.config/opencode/ — never committed
.opencode/

# IDE config (Cursor), not a project file
.cursorignore
