// Commands in this directory are shebang mains (import-contact.go,
// reconcile-contact.go, …), each behind an exclusive build tag so
// `go build ./bin/onlyoffice` does not see two mains.
package main
